CREATE TABLE Drops (
    Source VARCHAR(255) NOT NULL,
    ID INT NOT NULL,
    OldData JSON NOT NULL,
    Author INT NOT NULL,
    DateDropped DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (Author) REFERENCES Employees(ROWID)
)